# Core GUI
PyQt5>=5.15

# AI providers
anthropic>=0.100.0
openai>=2.0.0

# API server
fastapi>=0.100.0
uvicorn>=0.20.0

# Utilities
python-dotenv>=1.0.0
psutil>=5.9.0
PyYAML>=6.0
langdetect>=1.0.9

# Optional — local model inference (large download, ~2GB+)
# torch
# transformers

# Optional — GPU monitoring (NVIDIA only)
# pynvml

# Optional — conversation encryption
# cryptography
